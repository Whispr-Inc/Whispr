spring:
  application:
    name: service-discovery

server:
  port: 8761

eureka:
  server:
    enable-self-preservation: false       # In development, disable self-preservation to allow for quick registration and deregistration of services.
  client:
    register-with-eureka: false
    fetch-registry: false
